/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
      base_url: "https://borrow-uat.wizr.in/sentinel/"
      // base_url: "http://localhost:3000/sentinel/"
    }
  };
  
  export default nextConfig;
  
